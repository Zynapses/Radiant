% RADIANT Documentation PDF Template
% Used by assemble-complete-documentation.py via pandoc + xelatex
\documentclass[$if(fontsize)$$fontsize$,$endif$]{report}

% ── Page geometry ──
\usepackage[margin=0.75in, top=1in, bottom=1in]{geometry}

% ── Font setup (XeLaTeX — use system Helvetica via fontspec) ──
\usepackage{fontspec}
\defaultfontfeatures{Ligatures=TeX}
\setmainfont{Helvetica}
\setmonofont{Menlo}[Scale=0.85]

% ── Colors ──
\usepackage[dvipsnames,svgnames]{xcolor}
\definecolor{radiantblue}{HTML}{1E3A5F}
\definecolor{radiantaccent}{HTML}{2563EB}
\definecolor{codebg}{HTML}{F8F8F8}
\definecolor{rulegray}{HTML}{D1D5DB}
\definecolor{textgray}{HTML}{374151}
\definecolor{lightgray}{HTML}{9CA3AF}

% ── Headers and footers ──
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\color{lightgray}\nouppercase{\leftmark}}
\fancyhead[R]{\small\color{lightgray}RADIANT \& Think Tank}
\fancyfoot[C]{\small\color{lightgray}\thepage}
\fancyfoot[R]{\small\color{lightgray}Zynapses Inc.}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.2pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{rulegray}\leaders\hrule height \headrulewidth\hfill}}
\renewcommand{\footrule}{\hbox to\headwidth{\color{rulegray}\leaders\hrule height \footrulewidth\hfill}}

\fancypagestyle{plain}{
  \fancyhf{}
  \fancyfoot[C]{\small\color{lightgray}\thepage}
  \fancyfoot[R]{\small\color{lightgray}Zynapses Inc.}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0.2pt}
  \renewcommand{\footrule}{\hbox to\headwidth{\color{rulegray}\leaders\hrule height \footrulewidth\hfill}}
}

% ── Section styling ──
\usepackage{titlesec}
\titleformat{\chapter}[display]
  {\normalfont\huge\bfseries\color{radiantblue}}
  {\chaptertitlename\ \thechapter}{20pt}{\Huge}
\titleformat{\section}
  {\normalfont\Large\bfseries\color{radiantblue}}
  {\thesection}{1em}{}
\titleformat{\subsection}
  {\normalfont\large\bfseries\color{radiantblue!80!black}}
  {\thesubsection}{1em}{}
\titleformat{\subsubsection}
  {\normalfont\normalsize\bfseries\color{radiantblue!60!black}}
  {\thesubsubsection}{1em}{}

% ── Spacing ──
\setlength{\parskip}{0.4em}
\setlength{\parindent}{0pt}
\usepackage{setspace}
\setstretch{1.08}

% ── Links ──
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=radiantaccent,
  urlcolor=radiantaccent,
  citecolor=radiantaccent,
  pdfauthor={Zynapses Inc.},
  pdftitle={$title$},
  pdfsubject={RADIANT Complete Documentation},
  pdfkeywords={RADIANT, Think Tank, AI, SaaS}
}

% ── Code blocks (pandoc --listings mode) ──
\usepackage{listings}
\lstset{
  backgroundcolor=\color{codebg},
  basicstyle=\ttfamily\small,
  breaklines=true,
  breakatwhitespace=false,
  frame=single,
  rulecolor=\color{rulegray},
  framesep=4pt,
  xleftmargin=4pt,
  xrightmargin=4pt,
  tabsize=2,
  showstringspaces=false,
  aboveskip=0.8em,
  belowskip=0.8em,
  keepspaces=true,
  columns=flexible,
}

% ── Tables ──
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{array}
\renewcommand{\arraystretch}{1.3}

% ── Graphics ──
\usepackage{graphicx}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>\textheight\textheight\else\Gin@nat@height\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}

% ── Lists ──
\usepackage{enumitem}
\setlist[itemize]{topsep=3pt, partopsep=0pt, parsep=1pt, itemsep=2pt}
\setlist[enumerate]{topsep=3pt, partopsep=0pt, parsep=1pt, itemsep=2pt}

% ── Tight lists (pandoc) ──
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% ── CSL refs (pandoc) ──
$if(csl-refs)$
\newlength{\cslhangindent}
\setlength{\cslhangindent}{1.5em}
\newenvironment{CSLReferences}[2]{}{}
$endif$

% ── Title ──
$if(title)$
\title{\color{radiantblue}\textbf{$title$}}
$endif$
$if(author)$
\author{$author$}
$endif$
\date{$date$}

\begin{document}

% ── Custom title page ──
\begin{titlepage}
\centering
\vspace*{3cm}
{\color{radiantblue}\rule{\textwidth}{2pt}}\\[1.5cm]
{\Huge\bfseries\color{radiantblue} RADIANT \& Think Tank\\[0.5cm]}
{\LARGE Complete Documentation\\[0.3cm]}
{\large\color{textgray} All-in-One Reference Guide\\[2cm]}
{\color{radiantblue}\rule{\textwidth}{0.5pt}}\\[1cm]
{\large\color{textgray}
\begin{tabular}{rl}
\textbf{Version:} & $if(version)$$version$$else$4.18.0$endif$ \\[4pt]
\textbf{Generated:} & \today \\[4pt]
\textbf{Author:} & Zynapses Inc. \\[4pt]
\textbf{Classification:} & Confidential \\
\end{tabular}
}\\[2cm]
{\color{radiantblue}\rule{\textwidth}{2pt}}\\[1cm]
{\small\color{lightgray} Auto-generated from 18 consolidated documentation files\\
by \texttt{tools/scripts/assemble-complete-documentation.py}}
\vfill
\end{titlepage}

% ── Table of Contents ──
{
\hypersetup{linkcolor=black}
\tableofcontents
}
\clearpage

$body$

\end{document}
